version: 1.2

jobs:
  build:
    docker:
      - image: circleci/python:3.6
    steps:
      - run:
          name: Install Lektor
          command: |

            pip install Lektor
            lektor build && lektor deploy ghpages
